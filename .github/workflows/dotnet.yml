name: .NET 10 Build & Check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Setup .NET 10 SDK
      uses: actions/setup-dotnet@v4
      with:
        # '10.0.x' holt automatisch das neueste Patch-Release
        dotnet-version: '10.0.x'

    - name: Restore Dependencies
      run: dotnet restore ./mcp-dotnet-websearch.sln

    - name: Build
      # --no-restore spart Zeit, da wir es im Schritt davor gemacht haben
      run: dotnet build ./mcp-dotnet-websearch.sln --no-restore --configuration Release

    - name: Test
      # Führt Unit-Tests aus, falls vorhanden
      run: dotnet test ./mcp-dotnet-websearch.sln --no-build --configuration Release --verbosity normal
